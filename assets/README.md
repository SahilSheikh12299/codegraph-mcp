# Assets

Place static media for the repo here.

| File | Purpose |
|------|---------|
| `demo.mp4` | Product demo (embedded in root README) |
| `mascot.png` | Repo mascot / social preview (optional; link from README when ready) |

`demo.mp4` is not committed by default — add it locally, then commit if you want it on GitHub. For large files (>50 MB), consider [Git LFS](https://git-lfs.github.com/) or hosting on GitHub Releases and linking from the README instead.
